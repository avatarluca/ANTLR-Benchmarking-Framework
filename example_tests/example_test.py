import unittest


class GrammarTransformTest(unittest.TestCase):
    """
        Example test case for the grammar transformation.
        It depends on the output of the grammar transformation (is it just the parsetree or a intermediate representation?).
    """
    
    def test_example(self):
        assert True